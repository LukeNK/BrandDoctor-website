{
  "scripts": {
    "servePreview": "node Static-Wind/preview.js",
    "build": "cd Static-Wind && node build.js",
    "serveBuild": "cd Static-Wind && npm run serveBuild",
    "test": "cd Static-Wind && node build.js && cd build && npx http-server ./",
    "release": "cd Static-Wind && node build.js R"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  },
  "dependencies": {
    "pug": "^3.0.2"
  }
}
